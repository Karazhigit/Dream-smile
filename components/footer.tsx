import Link from "next/link";
import { clinicConfig } from "@/lib/clinic-config";
export function Footer(){return <footer className="bg-ink py-12 text-white"><div className="container-page flex flex-col gap-8 sm:flex-row sm:items-end sm:justify-between"><div><p className="font-serif text-3xl">{clinicConfig.name}</p><p className="mt-2 text-xs font-bold uppercase tracking-[.16em] text-white/50">Стоматология • {clinicConfig.city}</p></div><nav aria-label="Навигация в подвале" className="flex flex-wrap gap-x-6 gap-y-3">{clinicConfig.navigation.map(({label,href})=>href.startsWith("/")?<Link key={href} href={href} className="focus-ring text-sm text-white/65 transition hover:text-white">{label}</Link>:<a key={href} href={href} className="focus-ring text-sm text-white/65 transition hover:text-white">{label}</a>)}</nav></div></footer>}
