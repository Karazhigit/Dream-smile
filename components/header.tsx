"use client";
import { useState } from "react";
import { CloseIcon, MenuIcon } from "./icons";
const links=[["Услуги","#services"],["Специалисты","#specialists"],["О клинике","#about"],["Запись","#booking"],["Контакты","#contacts"]];
export function Header(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-line/80 bg-background/95 backdrop-blur-sm"><div className="container-page flex h-[78px] items-center justify-between gap-6"><a href="#top" aria-label="Dream Smile — на главную" className="focus-ring leading-none"><span className="block font-serif text-[25px] tracking-[-.04em]">Dream Smile</span><span className="mt-1 block text-[8px] font-extrabold tracking-[.26em] text-primary">СТОМАТОЛОГИЯ</span></a><nav aria-label="Главная навигация" className="hidden items-center gap-7 lg:flex">{links.map(([label,href])=><a key={href} href={href} className="focus-ring text-[13px] font-bold text-muted transition hover:text-primary">{label}</a>)}</nav><a href="#booking" className="btn-primary focus-ring hidden !min-h-[46px] lg:inline-flex">Записаться</a><button className="focus-ring grid h-11 w-11 place-items-center rounded-full border border-line bg-white lg:hidden" aria-label={open?"Закрыть меню":"Открыть меню"} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<CloseIcon/>:<MenuIcon/>}</button></div>{open&&<nav aria-label="Мобильная навигация" className="border-t border-line bg-white px-5 py-4 lg:hidden">{links.map(([label,href])=><a key={href} href={href} onClick={()=>setOpen(false)} className="focus-ring block border-b border-line py-3 text-sm font-bold last:border-0">{label}</a>)}<a href="#booking" onClick={()=>setOpen(false)} className="btn-primary focus-ring mt-4">Записаться</a></nav>}</header>}
