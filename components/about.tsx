import { FadeInSection } from "./motion";
import { clinicConfig } from "@/lib/clinic-config";
export function About(){return <FadeInSection id="about" className="section-pad bg-primary-dark text-white"><div className="container-page grid gap-12 lg:grid-cols-[.9fr_1.1fr] lg:gap-24"><div><p className="eyebrow !text-secondary">{clinicConfig.about.eyebrow}</p><h2 className="section-title mt-4">{clinicConfig.about.title}</h2></div><div><p className="text-lg leading-8 text-white/75">{clinicConfig.name} — {clinicConfig.about.description}</p><div className="mt-10 divide-y divide-white/15 border-y border-white/15">{clinicConfig.clinicFeatures.map((feature,i)=><div key={feature} className="flex items-center gap-5 py-5"><span className="text-xs font-extrabold text-accent">{String(i+1).padStart(2,"0")}</span><span className="font-bold">{feature}</span></div>)}</div></div></div></FadeInSection>}
