import { doctors } from "@/lib/data";
import { FadeInSection } from "./motion";
export function Specialists(){return <FadeInSection id="specialists" className="section-pad"><div className="container-page"><p className="eyebrow">Команда</p><h2 className="section-title mt-4">Наши специалисты</h2><p className="mt-5 max-w-xl text-sm leading-7 text-muted">Профили специалистов появятся после подтверждения информации клиникой.</p><div className="mt-10 grid gap-5 md:grid-cols-3">{doctors.map((doctor,index)=><article key={doctor.id} className="motion-card group overflow-hidden rounded-[24px] border border-line bg-white"><div className="flex aspect-[5/3] items-end bg-secondary p-6"><span className="font-serif text-6xl text-primary/20 transition-transform duration-200 group-hover:scale-[1.03]">0{index+1}</span></div><div className="p-6"><h3 className="text-lg font-bold">{doctor.role}</h3><p className="mt-3 text-sm leading-6 text-muted">{doctor.note}</p></div></article>)}</div></div></FadeInSection>}
