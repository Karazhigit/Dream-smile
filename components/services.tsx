import Link from "next/link";
import { clinicConfig } from "@/lib/clinic-config";
import { Arrow } from "./icons";
import { FadeInSection } from "./motion";
export function Services(){return <FadeInSection id="services" className="section-pad bg-white"><div className="container-page"><div className="grid gap-6 border-b border-line pb-10 md:grid-cols-2 md:items-end"><div><p className="eyebrow">Направления</p><h2 className="section-title mt-4">Наши услуги</h2></div><p className="max-w-lg text-sm leading-7 text-muted md:justify-self-end">Подберём подходящее направление после консультации и диагностики. Стоимость зависит от индивидуального плана лечения.</p></div><div>{clinicConfig.servicesDescription.map((service,index)=><Link href={clinicConfig.bookingHref} key={service} className="motion-card group grid gap-3 border-b border-line py-6 hover:bg-background sm:grid-cols-[48px_1fr_30px] sm:items-center sm:px-4"><span className="text-xs font-bold text-accent">{String(index+1).padStart(2,"0")}</span><h3 className="text-lg font-bold sm:text-xl">{service}</h3><Arrow className="h-5 w-5 text-primary transition-transform group-hover:translate-x-1"/></Link>)}</div><Link href={clinicConfig.bookingHref} className="btn-primary focus-ring mt-9">{clinicConfig.cta.consultation} <Arrow/></Link></div></FadeInSection>}
