export function normalizeKazakhstanPhone(value:string){const trimmed=value.trim();if(!/^[+\d\s()-]+$/.test(trimmed))return null;const digits=trimmed.replace(/\D/g,"");if(digits.length!==11||!(digits.startsWith("7")||digits.startsWith("8")))return null;return `+7${digits.slice(1)}`}
export function formatKazakhstanPhone(value:string){let digits=value.replace(/\D/g,"");if(digits.startsWith("8"))digits=`7${digits.slice(1)}`;if(!digits.startsWith("7"))digits=`7${digits}`;digits=digits.slice(0,11);const parts=[digits.slice(1,4),digits.slice(4,7),digits.slice(7,9),digits.slice(9,11)].filter(Boolean);return `+7${parts[0]?` ${parts[0]}`:""}${parts[1]?` ${parts[1]}`:""}${parts[2]?` ${parts[2]}`:""}${parts[3]?` ${parts[3]}`:""}`}
